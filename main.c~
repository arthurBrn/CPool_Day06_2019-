/*
** EPITECH PROJECT, 2019
** main
** File description:
** simply main
*/

#include <stdio.h>
#include <string.h>

char *my_strstr(char *str, char const *to_find);
int my_putstr(char const *str);

int main(void)
{
    char *str = "hello";
    char const *to_find = "";
    my_putstr(my_strstr(strdup(str), strdup(to_find)));
}
